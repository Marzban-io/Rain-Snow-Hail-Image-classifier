# Results

Figures generated by `notebooks/rain_snow_hail_classifier.ipynb`:

- `training_curves.png` — loss and accuracy per epoch across both fine-tuning phases
- `confusion_matrix.png` — per-class performance on the held-out test split

Both are referenced from the top-level README, so committing them here makes them
render on the repository front page.
